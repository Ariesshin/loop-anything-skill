# Loop v0: Receipt splitter reliability

# Loop v0：账单拆分工具可靠性

`runs/loop/v0.py` is the same first-draft implementation as `runs/control/receipt_splitter.py` before Loop revision.

`runs/loop/v0.py` 是进入 Loop 修改前的同一份初稿实现，与 `runs/control/receipt_splitter.py` 相同。

Baseline test result before Loop:

- Passed tests: 5/11
- Raw score: 25/55
- Normalized score: 45.5/100

Main failures before Loop:

- Discount is not applied before tax/tip proportionally.
- Rounding cents do not reconcile to the displayed grand total.
- Unknown people and empty participant lists produce unclear runtime errors.
- Negative prices and invalid rates are accepted instead of rejected.

Loop target:

Make the tool obey the named human-readable rules and pass all T01–T11 deterministic tests without changing the task or hiding failures.